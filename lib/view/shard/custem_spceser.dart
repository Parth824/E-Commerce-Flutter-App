import 'package:flutter/material.dart';

class CustemSpcer extends StatelessWidget {
  const CustemSpcer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 18,
    );
  }
}